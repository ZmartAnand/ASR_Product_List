specVersion: "v1"
serviceId: "asr-works"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "asr-works-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
